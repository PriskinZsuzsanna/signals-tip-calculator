export interface Button {
  amount: string,
  icon?: string,
  background?: string;
  color?: string;
}

export interface TotalRow {
  label: string,
  labelIdentifier: string,
  icon: string,
  amount: number
}